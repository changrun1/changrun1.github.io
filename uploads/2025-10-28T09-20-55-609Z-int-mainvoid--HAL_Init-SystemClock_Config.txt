int main(void)
{

  HAL_Init();

  SystemClock_Config();
  MX_GPIO_Init();
  MX_USART1_UART_Init();

	LCD_Init();
	LCD_Clear();
	char c[2] = {0, 0};
	
	while(1)	{
		HAL_UART_Receive(&huart1, (uint8_t *)c, 1, 0xFFFFFFFF);
		printf("%c", c[0]);
		LCD_DrawString(0, 0, c, 10);
	}
}

c[2]不能當*c用，不要用動態記憶體配置怎麼解決


